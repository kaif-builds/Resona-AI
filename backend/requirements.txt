# Resona AI Backend — Python Dependencies
# Railway will install these on deploy
# Requires Python 3.11 (Coqui TTS supports 3.9-3.11)

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# Coqui TTS (XTTS-v2 — zero-shot voice cloning, no fine-tuning needed)
TTS==0.22.0

# Deep learning (CPU build — Railway free tier has no GPU)
torch==2.2.2+cpu
torchaudio==2.2.2+cpu
--extra-index-url https://download.pytorch.org/whl/cpu

# Audio processing
librosa==0.10.2
soundfile==0.12.1
numpy==1.26.4

# Utilities
pydantic==2.7.1
httpx==0.27.0
