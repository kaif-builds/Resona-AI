# Resona AI Backend — Python Dependencies
# Railway will install these on deploy
# Requires Python 3.11 (Coqui TTS supports 3.9-3.11)

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# Coqui TTS (XTTS-v2 — zero-shot voice cloning, no fine-tuning needed)
TTS==0.22.0

# Deep learning
torch==2.4.0
torchaudio==2.4.0

# Audio processing
librosa==0.10.2
soundfile==0.12.1
numpy==1.26.4

# Transformers (pinned for Coqui TTS 0.22.0 compatibility — BeamSearchScorer removed in >=4.48)
transformers<4.48.0

# Utilities
pydantic==2.7.1
httpx==0.27.0
